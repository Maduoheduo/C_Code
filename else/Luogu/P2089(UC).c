#include <stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    long long cnt = 0;
    int flag = 1;
    if (n < 10 || n > 30)
    {
        flag = 0;
        printf("0");
        return 0;
    }

    return 0;
}