#include <stdio.h>
int dp[1005][1005], a[1005][1005];
int max(int i)
{
}
int main()
{
    int r;
    scanf("%d", &r);
    for (int i = 0; i < r; i++)
    {
        for (int j = 0; j < r; j++)
        {
            scanf("%d", &a[i][j]);
        }
    }
    for (int i = 0; i < r; i++)
    {
        dp[]
    }
    for (int i = r - 2; i >= 0; i++)
    {
    }
    return 0;
}