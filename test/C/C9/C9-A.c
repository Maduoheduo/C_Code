#include <stdio.h>
int main()
{
    int k;
    scanf("%d", &k);
    if (k == 1)
    {
        printf("No legacy is so rich as honesty.");
    }
    else if (k == 2)
    {
        printf("No legacy is so rich as honesty.\nIf one loses honesty, he will lose everything.\n");
    }
    else if (k == 3)
    {
        printf("No legacy is so rich as honesty.\nIf one loses honesty, he will lose everything.\nI promise I do not cheat in the exam.");
    }
    return 0;
}